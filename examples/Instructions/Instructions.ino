#include "MultiWatchy.h"
#include "settings.h"

// This demo shows instructions for using MultiWatchy, the default behavior
// if you don't add any watch faces to the carousel.

MultiWatchy watchy(settings);

void setup() {
  watchy.init();
}

void loop() {
}
